# Contributing

## Development

```bash
bun install
bun run typecheck
bun test
bun run build
```

This repo uses the pinned workspace TypeScript version (`typescript@^6.0.3`) via local `tsc` scripts.
